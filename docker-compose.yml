services:
  bot:
    build: .
    container_name: telegram_shop_bot
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: ${DB_PATH:-/app/data/shop.db}
    volumes:
      - ./data:/app/data